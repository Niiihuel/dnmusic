diff --git a/node_modules/expo-audio/android/src/main/java/expo/modules/audio/AudioModule.kt b/node_modules/expo-audio/android/src/main/java/expo/modules/audio/AudioModule.kt
index 992239b..83ad312 100644
--- a/node_modules/expo-audio/android/src/main/java/expo/modules/audio/AudioModule.kt
+++ b/node_modules/expo-audio/android/src/main/java/expo/modules/audio/AudioModule.kt
@@ -531,6 +531,12 @@ class AudioModule : Module() {
         }
       }
 
+      Function("setEqualizer") { player: AudioPlayer, enabled: Boolean, gains: List<Double> ->
+        runOnMain {
+          player.setEqualizer(enabled, gains)
+        }
+      }
+
       AsyncFunction("seekTo") { player: AudioPlayer, seekTime: Double, _: Double?, _: Double? ->
         player.seekTo(seekTime)
       }.runOnQueue(Queues.MAIN)
diff --git a/node_modules/expo-audio/android/src/main/java/expo/modules/audio/AudioPlayer.kt b/node_modules/expo-audio/android/src/main/java/expo/modules/audio/AudioPlayer.kt
index c76a9fd..19783df 100644
--- a/node_modules/expo-audio/android/src/main/java/expo/modules/audio/AudioPlayer.kt
+++ b/node_modules/expo-audio/android/src/main/java/expo/modules/audio/AudioPlayer.kt
@@ -4,6 +4,7 @@ import android.Manifest
 import android.content.Context
 import android.content.pm.PackageManager
 import android.media.audiofx.Visualizer
+import android.media.audiofx.Equalizer
 import android.util.Log
 import androidx.core.content.ContextCompat
 import androidx.media3.common.AudioAttributes
@@ -78,6 +79,15 @@ class AudioPlayer(
 
   private var samplingEnabled = false
   private var visualizer: Visualizer? = null
+  private var equalizer: Equalizer? = null
+  private var equalizerSessionId = C.AUDIO_SESSION_ID_UNSET
+  private var equalizerEnabled = false
+  private var equalizerGains = FloatArray(10)
+  private val equalizerListener = object : Player.Listener {
+    override fun onAudioSessionIdChanged(audioSessionId: Int) {
+      if (equalizerEnabled && audioSessionId != equalizerSessionId) rebuildEqualizer(audioSessionId)
+    }
+  }
   private val context by lazy {
     appContext.reactContext
       ?: throw Exceptions.ReactContextLost()
@@ -85,6 +95,7 @@ class AudioPlayer(
 
   init {
     installPlayerListeners()
+    ref.addListener(equalizerListener)
     source?.let {
       setMediaSource(source)
     }
@@ -180,6 +191,65 @@ class AudioPlayer(
     }
   }
 
+  /**
+   * Maps the app's stable ten-band curve onto the bands exposed by the device's
+   * AudioEffect implementation. Android equalizers vary by vendor, so each
+   * native centre frequency samples the logarithmic ten-band curve.
+   */
+  fun setEqualizer(enabled: Boolean, gains: List<Double>) {
+    equalizerEnabled = enabled
+    equalizerGains = FloatArray(10) { index ->
+      (gains.getOrNull(index) ?: 0.0).toFloat().coerceIn(-12f, 12f)
+    }
+    if (!enabled) {
+      equalizer?.enabled = false
+      return
+    }
+    if (equalizer == null || equalizerSessionId != ref.audioSessionId) {
+      rebuildEqualizer(ref.audioSessionId)
+    } else {
+      applyEqualizerCurve()
+    }
+  }
+
+  private fun rebuildEqualizer(audioSessionId: Int) {
+    equalizer?.release()
+    equalizer = null
+    equalizerSessionId = C.AUDIO_SESSION_ID_UNSET
+    if (!equalizerEnabled || audioSessionId == C.AUDIO_SESSION_ID_UNSET) return
+    try {
+      equalizer = Equalizer(0, audioSessionId)
+      equalizerSessionId = audioSessionId
+      applyEqualizerCurve()
+      equalizer?.enabled = true
+    } catch (error: RuntimeException) {
+      Log.w(TAG, "Equalizer is unavailable for audio session $audioSessionId", error)
+      equalizer?.release()
+      equalizer = null
+    }
+  }
+
+  private fun applyEqualizerCurve() {
+    val effect = equalizer ?: return
+    val frequencies = doubleArrayOf(31.0, 62.0, 125.0, 250.0, 500.0, 1000.0, 2000.0, 4000.0, 8000.0, 16000.0)
+    val range = effect.bandLevelRange
+    val minimum = range[0].toInt()
+    val maximum = range[1].toInt()
+    for (band in 0 until effect.numberOfBands.toInt()) {
+      val hz = effect.getCenterFreq(band.toShort()) / 1000.0
+      val upper = frequencies.indexOfFirst { it >= hz }.let { if (it < 0) frequencies.lastIndex else it }
+      val lower = (upper - 1).coerceAtLeast(0)
+      val gain = if (lower == upper) equalizerGains[upper]
+      else {
+        val span = kotlin.math.ln(frequencies[upper] / frequencies[lower])
+        val mix = if (span == 0.0) 0.0 else kotlin.math.ln(hz.coerceAtLeast(frequencies[lower]) / frequencies[lower]) / span
+        (equalizerGains[lower] + (equalizerGains[upper] - equalizerGains[lower]) * mix.toFloat())
+      }
+      effect.setBandLevel(band.toShort(), (gain * 100f).toInt().coerceIn(minimum, maximum).toShort())
+    }
+    effect.enabled = equalizerEnabled
+  }
+
   override fun setPlaybackRate(rate: Float) {
     val playbackRate = rate.coerceIn(0.1f, 2.0f)
     val pitch = if (preservesPitch) 1f else playbackRate
@@ -283,6 +353,9 @@ class AudioPlayer(
     }
     serviceConnection.unbind()
     visualizer?.release()
+    ref.removeListener(equalizerListener)
+    equalizer?.release()
+    equalizer = null
     super.releasePlayer()
   }
 
diff --git a/node_modules/expo-audio/ios/AudioComponentRegistry.swift b/node_modules/expo-audio/ios/AudioComponentRegistry.swift
index 036d166..b347f2f 100644
--- a/node_modules/expo-audio/ios/AudioComponentRegistry.swift
+++ b/node_modules/expo-audio/ios/AudioComponentRegistry.swift
@@ -1,5 +1,8 @@
 import AVFoundation
 import Foundation
+#if os(iOS)
+import UIKit
+#endif
 
 class AudioComponentRegistry {
   private var players = [String: AudioPlayer]()
@@ -11,8 +14,67 @@ class AudioComponentRegistry {
 
   private let registryQueue = DispatchQueue(label: "expo.audio.registry", attributes: .concurrent)
 
+  #if os(iOS)
+  private var transitionTask: UIBackgroundTaskIdentifier = .invalid
+  private var transitionDeadline: DispatchWorkItem?
+  private var transitionGeneration = 0
+  #endif
+
   init() {}
 
+  deinit {
+    #if os(iOS)
+    transitionDeadline?.cancel()
+    let task = transitionTask
+    if task != .invalid {
+      DispatchQueue.main.async { UIApplication.shared.endBackgroundTask(task) }
+    }
+    #endif
+  }
+
+  // Own the task in the registry: JS replaces/releases the finished player
+  // before starting its successor. Never keep silent audio playing as a bridge.
+  func beginPlaybackTransition() {
+    #if os(iOS)
+    guard Thread.isMainThread else {
+      DispatchQueue.main.async { self.beginPlaybackTransition() }
+      return
+    }
+    guard UIApplication.shared.applicationState == .background,
+      transitionTask == .invalid else { return }
+
+    transitionGeneration += 1
+    let generation = transitionGeneration
+    transitionTask = UIApplication.shared.beginBackgroundTask(withName: "ExpoAudio playback transition") { [weak self] in
+      guard let self, self.transitionGeneration == generation else { return }
+      self.endPlaybackTransition()
+    }
+    guard transitionTask != .invalid else { return }
+    let task = transitionTask
+    let deadline = DispatchWorkItem { [weak self] in
+      guard let self, self.transitionTask == task else { return }
+      self.endPlaybackTransition()
+    }
+    transitionDeadline = deadline
+    DispatchQueue.main.asyncAfter(deadline: .now() + 20, execute: deadline)
+    #endif
+  }
+
+  func endPlaybackTransition() {
+    #if os(iOS)
+    guard Thread.isMainThread else {
+      DispatchQueue.main.async { self.endPlaybackTransition() }
+      return
+    }
+    transitionDeadline?.cancel()
+    transitionDeadline = nil
+    guard transitionTask != .invalid else { return }
+    let task = transitionTask
+    transitionTask = .invalid
+    UIApplication.shared.endBackgroundTask(task)
+    #endif
+  }
+
   func add(_ player: AudioPlayer) {
     registryQueue.async(flags: .barrier) {
       self.players[player.id] = player
@@ -54,6 +116,7 @@ class AudioComponentRegistry {
   #endif
 
   func removeAll() {
+    endPlaybackTransition()
     registryQueue.async(flags: .barrier) {
       self.players.values.forEach { $0.owningRegistry = nil }
       self.players.removeAll()
diff --git a/node_modules/expo-audio/ios/AudioModule.swift b/node_modules/expo-audio/ios/AudioModule.swift
index d265606..7c1f5eb 100644
--- a/node_modules/expo-audio/ios/AudioModule.swift
+++ b/node_modules/expo-audio/ios/AudioModule.swift
@@ -107,6 +107,7 @@ public class AudioModule: Module {
     }
 
     OnAppEntersForeground {
+      registry.endPlaybackTransition()
       if !shouldPlayInBackground {
         resumeAllPlayers()
       }
@@ -242,7 +243,7 @@ public class AudioModule: Module {
       }
 
       Function("pause") { player in
-        player.ref.pause()
+        player.pause()
         if !player.keepAudioSessionActive {
           deactivateSession()
         }
@@ -258,6 +259,10 @@ public class AudioModule: Module {
         }
       }
 
+      Function("setEqualizer") { (player: AudioPlayer, enabled: Bool, gains: [Double]) in
+        player.setEqualizer(enabled: enabled, gains: gains)
+      }
+
       Function("setActiveForLockScreen") { (player: AudioPlayer, active: Bool, metadata: Metadata?, options: LockScreenOptions?) in
         player.setActiveForLockScreen(active, metadata: metadata, options: options)
       }
@@ -600,6 +605,11 @@ public class AudioModule: Module {
         case .doNotMix, .mixWithOthers:
           playable.pause()
         }
+      } else if let player = playable as? AudioPlayer, player.keepAudioSessionActive {
+        // A persistent player can be waiting for JS recovery after an error.
+        // Cancel that intent without adding it to the system resume set.
+        player.wasPlaying = false
+        player.pause()
       }
     }
 
diff --git a/node_modules/expo-audio/ios/AudioPlayer.swift b/node_modules/expo-audio/ios/AudioPlayer.swift
index 0742943..c9ce25f 100644
--- a/node_modules/expo-audio/ios/AudioPlayer.swift
+++ b/node_modules/expo-audio/ios/AudioPlayer.swift
@@ -23,7 +23,17 @@ public class AudioPlayer: SharedRef<AVPlayer>, Playable {
     ref.rate == 0.0
   }
   var samplingEnabled = false
-  var keepAudioSessionActive = false
+  private var equalizerEnabled = false
+  private var equalizerGains = Array(repeating: 0.0, count: 10)
+  var keepAudioSessionActive = false {
+    didSet {
+      // A cached item may already be ready before this option is assigned.
+      // Prepare the visual tap before playback, including background starts.
+      if keepAudioSessionActive && !isPlaying {
+        installTap()
+      }
+    }
+  }
 
   var isLooping = false {
     didSet {
@@ -47,9 +57,13 @@ public class AudioPlayer: SharedRef<AVPlayer>, Playable {
   private var timeToken: Any?
   private var cancellables = Set<AnyCancellable>()
   private var endObserver: NSObjectProtocol?
+  private var failureObserver: NSObjectProtocol?
+  private weak var failedItem: AVPlayerItem?
+  private var playbackFailure: String?
 
   private var audioProcessor: AudioTapProcessor?
   private var tapInstalled = false
+  private weak var sampledItem: AVPlayerItem?
   private var shouldInstallAudioTap = false
   weak var owningRegistry: AudioComponentRegistry?
   var onPlaybackComplete: (() -> Void)?
@@ -100,6 +114,9 @@ public class AudioPlayer: SharedRef<AVPlayer>, Playable {
   }
 
   func play(at rate: Float) {
+    if keepAudioSessionActive && !isPlaying {
+      installTap()
+    }
     ref.actionAtItemEnd = isLooping ? .advance : .pause
     if isLooping {
       enqueueNextLoopItem()
@@ -119,24 +136,45 @@ public class AudioPlayer: SharedRef<AVPlayer>, Playable {
     }
     samplingEnabled = enabled
     if enabled {
+      // Foreground only enables delivery of samples from the prepared tap.
+      // Never rebuild a playing persistent item's audioMix for a visual.
+      if keepAudioSessionActive && isPlaying {
+        return
+      }
       if isLoaded {
         installTap()
       } else {
         shouldInstallAudioTap = true
       }
     } else {
-      uninstallTap()
+      // Keep the audio graph intact across AppState changes. The callback
+      // checks samplingEnabled before allocating frames or sending JS events.
       shouldInstallAudioTap = false
     }
   }
 
+  func setEqualizer(enabled: Bool, gains: [Double]) {
+    equalizerEnabled = enabled
+    equalizerGains = (0..<10).map { index in
+      min(12, max(-12, gains.indices.contains(index) ? gains[index] : 0))
+    }
+    if enabled && isLoaded {
+      installTap()
+    }
+    audioProcessor?.setEqualizerEnabled(enabled, gains: equalizerGains.map { NSNumber(value: $0) })
+  }
+
   func currentStatus() -> [String: Any?] {
     let currentDuration = ref.status == .readyToPlay ? duration : 0.0
     let rate = isPlaying ? ref.rate : currentRate
+    let itemFailed = ref.currentItem?.status == .failed
+    let error = ref.currentItem?.error?.localizedDescription
+      ?? (ref.currentItem != nil && failedItem === ref.currentItem ? playbackFailure : nil)
+      ?? (itemFailed ? "Unknown playback error" : nil)
     return [
       "id": id,
       "currentTime": currentTime,
-      "playbackState": statusToString(status: ref.status),
+      "playbackState": itemFailed || error != nil ? "failed" : statusToString(status: ref.status),
       "timeControlStatus": timeControlStatusString(status: ref.timeControlStatus),
       "reasonForWaitingToPlay": reasonForWaitingToPlayString(status: ref.reasonForWaitingToPlay),
       "mute": ref.isMuted,
@@ -150,7 +188,7 @@ public class AudioPlayer: SharedRef<AVPlayer>, Playable {
       "isBuffering": isBuffering,
       "isLive": isLive,
       "currentOffsetFromLive": currentOffsetFromLive,
-      "error": nil
+      "error": error
     ]
   }
 
@@ -198,22 +236,31 @@ public class AudioPlayer: SharedRef<AVPlayer>, Playable {
   }
 
   private func setupPublisher() {
+    ref.publisher(for: \.timeControlStatus)
+      .receive(on: DispatchQueue.main)
+      .sink { [weak self] status in
+        guard let self, self.keepAudioSessionActive,
+          status == .playing, self.isPlaying else { return }
+        self.owningRegistry?.endPlaybackTransition()
+      }
+      .store(in: &cancellables)
+
     ref.publisher(for: \.currentItem?.status)
       .sink { [weak self] status in
         guard let self, let status else {
           return
         }
         if status == .readyToPlay {
+          if shouldInstallAudioTap || samplingEnabled || keepAudioSessionActive {
+            installTap()
+            shouldInstallAudioTap = false
+          }
           self.updateStatus(with: [
             "isLoaded": true
           ])
           if self.isLooping {
             self.enqueueNextLoopItem()
           }
-          if shouldInstallAudioTap || samplingEnabled {
-            installTap()
-            shouldInstallAudioTap = false
-          }
         }
         if status == .failed {
           self.updateStatus(with: [
@@ -228,12 +275,14 @@ public class AudioPlayer: SharedRef<AVPlayer>, Playable {
         guard let self else {
           return
         }
+        self.addPlaybackFailureNotification()
         if self.isLooping {
           self.enqueueNextLoopItem()
           self.addPlaybackEndNotification()
         }
-        if self.samplingEnabled && self.isLoaded {
-          self.uninstallTap()
+        if (self.samplingEnabled || self.keepAudioSessionActive) && self.isLoaded {
+          // installTap is idempotent for the same AVPlayerItem. A ready-item
+          // event followed by currentItem must not tear down its fresh mix.
           self.installTap()
         }
       }
@@ -246,9 +295,7 @@ public class AudioPlayer: SharedRef<AVPlayer>, Playable {
     removeQueuedLoopItems()
     ref.pause()
 
-    if samplingEnabled {
-      uninstallTap()
-    }
+    uninstallTap()
     replacePlayerItem(with: item)
 
     if wasSamplingEnabled {
@@ -267,9 +314,7 @@ public class AudioPlayer: SharedRef<AVPlayer>, Playable {
     removeQueuedLoopItems()
     ref.pause()
 
-    if samplingEnabled {
-      uninstallTap()
-    }
+    uninstallTap()
     let item = AudioUtils.createAVPlayerItem(from: source)
     replacePlayerItem(with: item)
 
@@ -320,9 +365,7 @@ public class AudioPlayer: SharedRef<AVPlayer>, Playable {
 
   private func teardownPlayer() {
     removeQueuedLoopItems()
-    if samplingEnabled {
-      uninstallTap()
-    }
+    uninstallTap()
     if let timeToken {
       ref.removeTimeObserver(timeToken)
       self.timeToken = nil
@@ -331,6 +374,12 @@ public class AudioPlayer: SharedRef<AVPlayer>, Playable {
       NotificationCenter.default.removeObserver(endObserver)
       self.endObserver = nil
     }
+    if let failureObserver {
+      NotificationCenter.default.removeObserver(failureObserver)
+      self.failureObserver = nil
+    }
+    failedItem = nil
+    playbackFailure = nil
     cancellables.removeAll()
     ref.pause()
   }
@@ -358,7 +407,7 @@ public class AudioPlayer: SharedRef<AVPlayer>, Playable {
       return
     }
 
-    guard audioProcessor?.isTapInstalled != true else {
+    guard audioProcessor?.isTapInstalled != true || sampledItem !== ref.currentItem else {
       tapInstalled = true
       return
     }
@@ -370,8 +419,10 @@ public class AudioPlayer: SharedRef<AVPlayer>, Playable {
     audioProcessor = AudioTapProcessor(player: ref)
     let success = audioProcessor?.installTap() ?? false
     tapInstalled = success
+    sampledItem = success ? ref.currentItem : nil
 
     if success {
+      audioProcessor?.setEqualizerEnabled(equalizerEnabled, gains: equalizerGains.map { NSNumber(value: $0) })
       audioProcessor?.sampleBufferCallback = { [weak self] buffer, frameCount, timestamp in
         guard let self = self,
           let audioBuffer = buffer?.pointee,
@@ -400,6 +451,7 @@ public class AudioPlayer: SharedRef<AVPlayer>, Playable {
 
   private func uninstallTap() {
     tapInstalled = false
+    sampledItem = nil
     audioProcessor?.uninstallTap()
     audioProcessor?.sampleBufferCallback = nil
   }
@@ -439,6 +491,32 @@ public class AudioPlayer: SharedRef<AVPlayer>, Playable {
     }
   }
 
+  private func addPlaybackFailureNotification() {
+    if let failureObserver {
+      NotificationCenter.default.removeObserver(failureObserver)
+      self.failureObserver = nil
+    }
+    guard let item = ref.currentItem else { return }
+
+    // A terminal failure during playback has its own notification. Observe it
+    // as well as item.status so JS can recover a stream that stopped early.
+    failureObserver = NotificationCenter.default.addObserver(
+      forName: AVPlayerItem.failedToPlayToEndTimeNotification,
+      object: item,
+      queue: .main
+    ) { [weak self] notification in
+      guard let self,
+        let failedItem = notification.object as? AVPlayerItem,
+        failedItem === self.ref.currentItem else {
+        return
+      }
+      let error = notification.userInfo?[AVPlayerItemFailedToPlayToEndTimeErrorKey] as? NSError
+      self.failedItem = failedItem
+      self.playbackFailure = error?.localizedDescription ?? failedItem.error?.localizedDescription ?? "Playback failed before the end"
+      self.updateStatus(with: [:])
+    }
+  }
+
   private func addPlaybackEndNotification() {
     if let endObserver {
       NotificationCenter.default.removeObserver(endObserver)
@@ -447,7 +525,7 @@ public class AudioPlayer: SharedRef<AVPlayer>, Playable {
     endObserver = NotificationCenter.default.addObserver(
       forName: .AVPlayerItemDidPlayToEndTime,
       object: nil,
-      queue: nil
+      queue: .main
     ) { [weak self] notification in
       guard let self,
         let finishedItem = notification.object as? AVPlayerItem else {
@@ -460,6 +538,11 @@ public class AudioPlayer: SharedRef<AVPlayer>, Playable {
       }
 
       if !self.isLooping {
+        // The next item is selected in JS, which can outlive this player.
+        // Request a short native transition window before emitting its end.
+        if self.keepAudioSessionActive {
+          self.owningRegistry?.beginPlaybackTransition()
+        }
         let currentTime = finishedItem.duration.seconds
         self.updateStatus(with: [
           "playing": false,
@@ -497,6 +580,10 @@ public class AudioPlayer: SharedRef<AVPlayer>, Playable {
 
   func pause() {
     ref.pause()
+    if keepAudioSessionActive {
+      owningRegistry?.endPlaybackTransition()
+    }
+    updateStatus(with: ["didJustPause": true])
   }
 
   func resumePlayback() {
diff --git a/node_modules/expo-audio/ios/AudioTapProcessor.h b/node_modules/expo-audio/ios/AudioTapProcessor.h
index ddd4f23..73d5882 100644
--- a/node_modules/expo-audio/ios/AudioTapProcessor.h
+++ b/node_modules/expo-audio/ios/AudioTapProcessor.h
@@ -11,6 +11,7 @@ typedef void (^SampleBufferCallback)(AudioBuffer *audioBuffer, long frameCount,
 
 - (instancetype)initWithPlayer:(AVPlayer *)player;
 - (BOOL)installTap;
+- (void)setEqualizerEnabled:(BOOL)enabled gains:(NSArray<NSNumber *> *)gains;
 - (void)uninstallTap;
 - (void)invalidate;
 
diff --git a/node_modules/expo-audio/ios/AudioTapProcessor.m b/node_modules/expo-audio/ios/AudioTapProcessor.m
index 6f8fb16..4445c9c 100644
--- a/node_modules/expo-audio/ios/AudioTapProcessor.m
+++ b/node_modules/expo-audio/ios/AudioTapProcessor.m
@@ -3,14 +3,31 @@
 #import "AudioTapProcessor.h"
 #import <AudioToolbox/AudioToolbox.h>
 #import <os/lock.h>
+#import <math.h>
 
 NS_ASSUME_NONNULL_BEGIN
 
+#define DN_EQ_BANDS 10
+#define DN_EQ_MAX_CHANNELS 8
+
+typedef struct DNEQCoefficients {
+  double b0, b1, b2, a1, a2;
+} DNEQCoefficients;
+
+typedef struct DNEQState {
+  double x1, x2, y1, y2;
+} DNEQState;
+
 typedef struct AVAudioTapProcessorContext {
   Boolean isNonInterleaved;
   Boolean supportedTapProcessingFormat;
   void *self;
   Boolean isValid;
+  Float64 sampleRate;
+  UInt32 channelCount;
+  uint64_t equalizerVersion;
+  DNEQCoefficients equalizerCoefficients[DN_EQ_BANDS];
+  DNEQState equalizerState[DN_EQ_MAX_CHANNELS][DN_EQ_BANDS];
 } AVAudioTapProcessorContext;
 
 
@@ -19,6 +36,10 @@ typedef struct AVAudioTapProcessorContext {
   MTAudioProcessingTapRef _audioProcessingTap;
   BOOL _isTapInstalled;
   BOOL _isInvalidated;
+  os_unfair_lock _equalizerLock;
+  BOOL _equalizerEnabled;
+  float _equalizerGains[DN_EQ_BANDS];
+  uint64_t _equalizerVersion;
 }
 
 - (instancetype)initWithPlayer:(AVPlayer *)player {
@@ -29,10 +50,102 @@ typedef struct AVAudioTapProcessorContext {
     _isTapInstalled = NO;
     _isInvalidated = NO;
     _audioProcessingTap = NULL;
+    _equalizerLock = OS_UNFAIR_LOCK_INIT;
+    _equalizerEnabled = NO;
+    _equalizerVersion = 1;
+    memset(_equalizerGains, 0, sizeof(_equalizerGains));
   }
   return self;
 }
 
+- (void)setEqualizerEnabled:(BOOL)enabled gains:(NSArray<NSNumber *> *)gains {
+  os_unfair_lock_lock(&_equalizerLock);
+  _equalizerEnabled = enabled;
+  for (NSInteger index = 0; index < DN_EQ_BANDS; index++) {
+    double value = index < gains.count ? gains[index].doubleValue : 0.0;
+    _equalizerGains[index] = (float)fmax(-12.0, fmin(12.0, value));
+  }
+  _equalizerVersion++;
+  os_unfair_lock_unlock(&_equalizerLock);
+}
+
+static DNEQCoefficients DNEQMakePeaking(Float64 frequency, Float64 sampleRate, Float64 gainDB) {
+  const double nyquistSafe = sampleRate * 0.45;
+  const double f = fmax(20.0, fmin(frequency, nyquistSafe));
+  const double a = pow(10.0, gainDB / 40.0);
+  const double omega = 2.0 * M_PI * f / sampleRate;
+  const double alpha = sin(omega) / (2.0 * M_SQRT2);
+  const double cosOmega = cos(omega);
+  const double a0 = 1.0 + alpha / a;
+  DNEQCoefficients c;
+  c.b0 = (1.0 + alpha * a) / a0;
+  c.b1 = (-2.0 * cosOmega) / a0;
+  c.b2 = (1.0 - alpha * a) / a0;
+  c.a1 = (-2.0 * cosOmega) / a0;
+  c.a2 = (1.0 - alpha / a) / a0;
+  return c;
+}
+
+- (void)processEqualizer:(AVAudioTapProcessorContext *)context
+              bufferList:(AudioBufferList *)bufferList
+                   frames:(CMItemCount)frameCount {
+  BOOL enabled;
+  float gains[DN_EQ_BANDS];
+  uint64_t version;
+  os_unfair_lock_lock(&_equalizerLock);
+  enabled = _equalizerEnabled;
+  memcpy(gains, _equalizerGains, sizeof(gains));
+  version = _equalizerVersion;
+  os_unfair_lock_unlock(&_equalizerLock);
+
+  if (!enabled || !context->supportedTapProcessingFormat || frameCount <= 0) return;
+  if (context->equalizerVersion != version) {
+    static const double frequencies[DN_EQ_BANDS] = { 31, 62, 125, 250, 500, 1000, 2000, 4000, 8000, 16000 };
+    for (NSInteger band = 0; band < DN_EQ_BANDS; band++) {
+      context->equalizerCoefficients[band] = DNEQMakePeaking(frequencies[band], context->sampleRate, gains[band]);
+    }
+    memset(context->equalizerState, 0, sizeof(context->equalizerState));
+    context->equalizerVersion = version;
+  }
+
+  const UInt32 channels = MIN(context->channelCount, DN_EQ_MAX_CHANNELS);
+  if (context->isNonInterleaved) {
+    const UInt32 buffers = MIN(bufferList->mNumberBuffers, channels);
+    for (UInt32 channel = 0; channel < buffers; channel++) {
+      float *samples = (float *)bufferList->mBuffers[channel].mData;
+      if (!samples) continue;
+      for (CMItemCount frame = 0; frame < frameCount; frame++) {
+        double value = samples[frame];
+        for (NSInteger band = 0; band < DN_EQ_BANDS; band++) {
+          DNEQCoefficients c = context->equalizerCoefficients[band];
+          DNEQState *s = &context->equalizerState[channel][band];
+          const double output = c.b0 * value + c.b1 * s->x1 + c.b2 * s->x2 - c.a1 * s->y1 - c.a2 * s->y2;
+          s->x2 = s->x1; s->x1 = value; s->y2 = s->y1; s->y1 = output;
+          value = output;
+        }
+        samples[frame] = (float)fmax(-1.0, fmin(1.0, value));
+      }
+    }
+  } else if (bufferList->mNumberBuffers > 0) {
+    float *samples = (float *)bufferList->mBuffers[0].mData;
+    if (!samples) return;
+    for (CMItemCount frame = 0; frame < frameCount; frame++) {
+      for (UInt32 channel = 0; channel < channels; channel++) {
+        const CMItemCount sampleIndex = frame * channels + channel;
+        double value = samples[sampleIndex];
+        for (NSInteger band = 0; band < DN_EQ_BANDS; band++) {
+          DNEQCoefficients c = context->equalizerCoefficients[band];
+          DNEQState *s = &context->equalizerState[channel][band];
+          const double output = c.b0 * value + c.b1 * s->x1 + c.b2 * s->x2 - c.a1 * s->y1 - c.a2 * s->y2;
+          s->x2 = s->x1; s->x1 = value; s->y2 = s->y1; s->y1 = output;
+          value = output;
+        }
+        samples[sampleIndex] = (float)fmax(-1.0, fmin(1.0, value));
+      }
+    }
+  }
+}
+
 - (BOOL)isTapInstalled {
   os_unfair_lock_lock(&_lock);
   BOOL installed = _isTapInstalled;
@@ -168,7 +281,11 @@ void tapFinalize(MTAudioProcessingTapRef tap) {
 
 void tapPrepare(MTAudioProcessingTapRef tap, CMItemCount maxFrames, const AudioStreamBasicDescription *processingFormat) {
   AVAudioTapProcessorContext *context = (AVAudioTapProcessorContext *)MTAudioProcessingTapGetStorage(tap);
-  context->supportedTapProcessingFormat = true;
+  context->supportedTapProcessingFormat = processingFormat->mFormatID == kAudioFormatLinearPCM && (processingFormat->mFormatFlags & kAudioFormatFlagIsFloat) != 0;
+  context->sampleRate = processingFormat->mSampleRate;
+  context->channelCount = processingFormat->mChannelsPerFrame;
+  context->equalizerVersion = 0;
+  memset(context->equalizerState, 0, sizeof(context->equalizerState));
 
   if (processingFormat->mFormatID != kAudioFormatLinearPCM) {
     NSLog(@"Audio Format ID for audioProcessingTap: LinearPCM");
@@ -201,6 +318,8 @@ void tapProcess(MTAudioProcessingTapRef tap, CMItemCount numberFrames, MTAudioPr
     return;
   }
 
+  [processor processEqualizer:context bufferList:bufferListInOut frames:*numberFramesOut];
+
   SampleBufferCallback callback = processor.sampleBufferCallback;
   if (callback && bufferListInOut && bufferListInOut->mNumberBuffers > 0) {
     double timestamp = 0.0;
diff --git a/node_modules/expo-audio/ios/MediaController.swift b/node_modules/expo-audio/ios/MediaController.swift
index e977f57..2d056a2 100644
--- a/node_modules/expo-audio/ios/MediaController.swift
+++ b/node_modules/expo-audio/ios/MediaController.swift
@@ -246,7 +246,7 @@ class MediaController {
         return .commandFailed
       }
 
-      player.ref.pause()
+      player.pause()
       return .success
     }
 
@@ -256,7 +256,7 @@ class MediaController {
       }
 
       if player.isPlaying {
-        player.ref.pause()
+        player.pause()
       } else {
         player.play(at: Float(player.currentRate > 0 ? player.currentRate : 1.0))
       }
diff --git a/node_modules/expo-audio/src/AudioModule.types.ts b/node_modules/expo-audio/src/AudioModule.types.ts
index 7e75371..068b0e7 100644
--- a/node_modules/expo-audio/src/AudioModule.types.ts
+++ b/node_modules/expo-audio/src/AudioModule.types.ts
@@ -208,6 +208,13 @@ export declare class AudioPlayer extends SharedObject<AudioEvents> {
    */
   setAudioSamplingEnabled(enabled: boolean): void;
 
+  /**
+   * Applies a ten-band graphic equalizer to this player.
+   * Gains are expressed in dB for 31, 62, 125, 250, 500, 1k, 2k, 4k, 8k and 16k Hz.
+   * @hidden
+   */
+  setEqualizer(enabled: boolean, gains: number[]): void;
+
   /**
    * Sets or removes this audio player as the active player for lock screen controls.
    * Only one player can control the lock screen at a time.
diff --git a/node_modules/expo-audio/src/AudioPlayer.web.ts b/node_modules/expo-audio/src/AudioPlayer.web.ts
index 61db2df..733bbe6 100644
--- a/node_modules/expo-audio/src/AudioPlayer.web.ts
+++ b/node_modules/expo-audio/src/AudioPlayer.web.ts
@@ -22,6 +22,7 @@ import { mediaSessionController } from './MediaSessionController.web';
 export const activePlayers = new Set<AudioPlayerWeb>();
 
 let hasWarnedIOSVolume = false;
+const EQUALIZER_FREQUENCIES = [31, 62, 125, 250, 500, 1000, 2000, 4000, 8000, 16000];
 function isIOSSafari(): boolean {
   return /iPad|iPhone|iPod/.test(navigator.userAgent);
 }
@@ -53,6 +54,9 @@ export class AudioPlayerWeb
   private crossOrigin?: 'anonymous' | 'use-credentials';
   private analyser: AnalyserNode | null = null;
   private sourceNode: MediaElementAudioSourceNode | null = null;
+  private equalizerNodes: BiquadFilterNode[] = [];
+  private equalizerEnabled = false;
+  private equalizerGains = Array(10).fill(0) as number[];
   private samplingFrameId: number | null = null;
   private samplingEnabled = false;
   private samplingBuffer: Float32Array<ArrayBuffer> | null = null;
@@ -148,6 +152,12 @@ export class AudioPlayerWeb
     this.loaded = false;
     this.media = this._createMediaElement();
 
+    // createMediaElementSource belongs to the element it was created for.
+    // Reconnect the persisted EQ curve to the replacement element as well.
+    if (this.equalizerEnabled) {
+      this.setEqualizer(true, this.equalizerGains);
+    }
+
     if (wasSampling) {
       this.setAudioSamplingEnabled(true);
     }
@@ -197,9 +207,7 @@ export class AudioPlayerWeb
       this.analyser = ctx.createAnalyser();
       this.analyser.fftSize = 2048;
 
-      this.sourceNode.disconnect();
-      this.sourceNode.connect(this.analyser);
-      this.analyser.connect(ctx.destination);
+      this.reconnectAudioGraph();
 
       this.samplingBuffer = new Float32Array(this.analyser.frequencyBinCount);
       this.samplingEnabled = true;
@@ -216,17 +224,62 @@ export class AudioPlayerWeb
         this.analyser = null;
       }
 
-      if (this.sourceNode) {
-        const ctx = getAudioContext();
-        this.sourceNode.disconnect();
-        this.sourceNode.connect(ctx.destination);
-      }
+      if (this.sourceNode) this.reconnectAudioGraph();
 
       this.samplingBuffer = null;
       this.samplingEnabled = false;
     }
   }
 
+  setEqualizer(enabled: boolean, gains: number[]): void {
+    this.equalizerEnabled = enabled;
+    this.equalizerGains = Array.from({ length: 10 }, (_, index) =>
+      Math.max(-12, Math.min(12, Number.isFinite(gains[index]) ? gains[index] : 0))
+    );
+
+    if (!enabled && !this.sourceNode) return;
+    if (!this.media.crossOrigin && this._isCrossOrigin()) return;
+
+    const ctx = getAudioContext();
+    if (ctx.state === 'suspended') void ctx.resume();
+    if (!this.sourceNode) this.sourceNode = ctx.createMediaElementSource(this.media);
+    if (!this.equalizerNodes.length) {
+      this.equalizerNodes = EQUALIZER_FREQUENCIES.map((frequency) => {
+        const node = ctx.createBiquadFilter();
+        node.type = 'peaking';
+        node.frequency.value = frequency;
+        node.Q.value = Math.SQRT2;
+        return node;
+      });
+    }
+    this.equalizerNodes.forEach((node, index) => {
+      node.gain.setTargetAtTime(enabled ? this.equalizerGains[index] : 0, ctx.currentTime, 0.015);
+    });
+    this.reconnectAudioGraph();
+  }
+
+  private reconnectAudioGraph(): void {
+    if (!this.sourceNode) return;
+    const ctx = getAudioContext();
+    this.sourceNode.disconnect();
+    this.equalizerNodes.forEach((node) => node.disconnect());
+    this.analyser?.disconnect();
+
+    let tail: AudioNode = this.sourceNode;
+    if (this.equalizerEnabled && this.equalizerNodes.length) {
+      for (const node of this.equalizerNodes) {
+        tail.connect(node);
+        tail = node;
+      }
+    }
+    if (this.analyser) {
+      tail.connect(this.analyser);
+      this.analyser.connect(ctx.destination);
+    } else {
+      tail.connect(ctx.destination);
+    }
+  }
+
   private startSampling(): void {
     if (!this.samplingEnabled || !this.analyser || !this.samplingBuffer) {
       return;
@@ -375,7 +428,15 @@ export class AudioPlayerWeb
     };
 
     media.onended = () => {
+      this.isPlaying = false;
       lastEmitTime = 0;
+      // Deliver completion even when the final timeupdate was throttled or JS
+      // animation frames are suspended in a hidden browser tab.
+      this.emit(PLAYBACK_STATUS_UPDATE, {
+        ...getStatusFromMedia(media, this.id),
+        playing: false,
+        didJustFinish: true,
+      });
       mediaSessionController.updatePlaybackState(this);
     };
 
