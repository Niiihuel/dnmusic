import type { FiltrosCatalogoPerfilProps } from './FiltrosCatalogoPerfil.types'

/** The shared catalog keeps its existing desktop controls. */
export function FiltrosCatalogoPerfil(_props: FiltrosCatalogoPerfilProps) { return null }
